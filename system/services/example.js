// The application config is passed in.
module.exports = async function (config) {
  // Anything that is returned is available in your substruct namespace under `substruct.services.[name_of_this_file]`.
  return {}
}
